# Voice assistant UI reference

The implementation is based on the [generated primary-screen reference](voice-assistant-ui-reference.png). The source image is a visual reference only; the controls in the app call the actual project APIs and do not simulate STT, LLM, or TTS output.
